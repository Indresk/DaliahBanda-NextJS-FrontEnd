import { apiWrapper } from '@/back/utils/apiWrapper';
import { handlers } from '@/back/config/auth';

export const { GET, POST } = handlers;

// export const GET = apiWrapper(handlers);
// export const POST = apiWrapper(handlers);
